type Query {
    orderDetails(orderId: ID!): OrderDetails
}

type OrderDetails {
    orderId: ID
    status: String
    totalPrice: Float
    createdAt: String
    customer: Customer
    items: [OrderItem]
}

type Customer {
    id: ID
    name: String
    email: String
    address: String
}

type OrderItem {
    productId: ID
    productName: String
    description: String
    quantity: Int
    unitPrice: Float
    totalPrice: Float
}